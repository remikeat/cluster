global:
  databasePassword: <path:kv/data/kubeclarity#db_password>
kubeclarity:
  ingress:
    enabled: true
    ingressClassName: tailscale
    hosts:
      - host: kubeclarity
    tls:
      - hosts:
          - kubeclarity
