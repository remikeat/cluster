service:
  type: ClusterIP
tls:
  force: true
ingress:
  enabled: true
  ingressClassName: tailscale
  hosts:
    - host: portainer
      paths:
        - path: "/"
  tls:
    - hosts:
        - portainer
