service:
  type: ClusterIP
tls:
  force: false
ingress:
  enabled: true
  ingressClassName: tailscale
  hosts:
    - host: portainer
      paths:
        - path: "/"
  tls:
    - hosts:
        - portainer
