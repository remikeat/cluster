apiVersion: v1
kind: ConfigMap
metadata:
  name: secret-config-plugin-config
  namespace: argo-cd
data:
  plugin.yaml: |
    apiVersion: argoproj.io/v1alpha1
    kind: ConfigManagementPlugin
    metadata:
      name: secret-config-plugin
    spec:
      init:
        command: [sh, -c]
        args:
          - |
            pwd
            cp /helm-values/*.yaml .
            ls -la
      generate:
        command: [sh, -c]
        args:
          - |
            echo $ARGOCD_ENV_VALUES
            ls -la
            helm template $ARGOCD_ENV_ARGS -f $ARGOCD_ENV_SECRETS -f $ARGOCD_ENV_VALUES -n $ARGOCD_APP_NAMESPACE $ARGOCD_APP_NAME .
